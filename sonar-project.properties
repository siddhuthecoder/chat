# Project identification
sonar.projectKey=dls_ops
sonar.organization=dls
sonar.projectName=Ops360_chat_Backend
sonar.projectVersion=1.0.0

# Source encoding
sonar.sourceEncoding=UTF-8


# Optional: if you have test files in a separate folder
# sonar.tests=tests
# sonar.test.inclusions=**/*.spec.ts,**/*.test.ts

# TypeScript settings (required for accurate analysis)
sonar.typescript.tsconfigPath=tsconfig.json

# Coverage report (e.g., from Jest with `jest --coverage`)
# sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Optional: set base directory (if sonar scanner is not run from root)
# sonar.projectBaseDir=.

# Optional for Monorepos:
# sonar.modules=package-a,package-b
# sonar.package-a.sources=packages/package-a/src
# sonar.package-b.sources=packages/package-b/src
